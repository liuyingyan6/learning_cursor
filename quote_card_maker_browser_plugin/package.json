{
  "name": "quote-card-generator",
  "version": "1.0.0",
  "description": "Chrome extension for generating beautiful quote cards from selected text",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-icons": "node scripts/generate-icons.js",
    "generate-backgrounds": "node scripts/generate-backgrounds.js",
    "generate-assets": "npm run generate-icons && npm run generate-backgrounds"
  },
  "keywords": [
    "chrome-extension",
    "quote-card",
    "text-selection"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "html2canvas": "^1.4.1"
  },
  "devDependencies": {
    "sharp": "^0.32.1"
  }
}
